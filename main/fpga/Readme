# Requirements
- make sure that you have installed the Vivado and add it to your $PATH. (We use 2020.2 Version)
- `cp f37x_board <path_to_vivado>/data/boards/board_files -r` to install the borad files to Vivado
